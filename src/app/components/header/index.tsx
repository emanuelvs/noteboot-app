import * as React from 'react'
import './header.scss';

const Header = () => {

    return (
        <header>
            <h1 id="app-name">Notebook</h1>
        </header>
    )
}

export default Header;